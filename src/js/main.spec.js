//import  { SVG }  from '@svgdotjs/svg.js'

describe("App", () => {
  it("Renders without error", () => {
    //const draww = SVG().addTo('#drawing').size('100%', '100%');
    //draww.rect().attr(option);
  });
});
